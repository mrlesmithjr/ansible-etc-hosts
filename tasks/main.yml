---
# tasks file for ansible-etc-hosts
- name: main | updating /etc/hosts (localhost)
  template:
    src: "etc/hosts.j2"
    dest: "/etc/hosts"
    owner: root
    group: root
    mode: 0644
  become: true
